py -V                                   // Проверка версии python
import sys; print sys.version           // Проверка версии python из кода
python.exe -m pip install --upgrade pip // Обновление pyven
python -m venv venv                     // Создание виртуального пространства
.\venv\Scripts\activate                 // Активация виртуального пространства (venv салатовым)
deactivate                              // Деактивация виртуального пространства
pip list                                // Список подключенных модулей
Get-ExecutionPolicy                     // Проверка доступа к скриптам
Set-ExecutionPolicy AllSigned           // (RemoteSigned) Разрешает работу скриптов
pip install Flask                       // Установка Flask
py -m pip freeze                        // Список подключенных модулей
pip freeze > requirements.txt           // Сохранить список подключенных модулей в файл
pip freeze -r requirements.txt          // Загрузить список подключенных модулей с файла
Создать файл gitignore.txt              // Добавление исключений для Flask, Python, VScode
app.py                                  // Точка входа
python app.py                           // Запуск сервера с терминала
Далее смотреть app.py...

Templates = папка для шаблонов
Static - папка для для css js image

План действий:
1. Перечислить странички которые нужны
2. Расписать под них функции
3. Оформить их в шаблоне
4. Сделать нормальную верстку
5. Дизайн
6. Создание, редактирование, удалить, список всего и вся

pip install flask_sqlalchemy            // Модуль для работы с БД
from flask_sqlalchemy import SQLAlchemy // Подключаем модуль
app.config["SQLALCHEMY_DATABASE_URI"] = "sqlite:///sqlite.db"
db = SQLAlchemy(app)

class User(db.Model):                   // Расписываем классы для таблиц
Переходим в режим питон
>>> from app import app, db             // Создание папки
>>> app.app_context().push()            // Пушим
>>> db.create_all()                     //

Вывод базы данных смотреть в user_list

SQLAlchemy - типы данных смореть в гугле

Реализацию методов CRUD - смореть в control.py

Виды базы данных:
1. 1 к 1
2. 1 ко многим
3. Многие ко многим (с нормализацией)

Подключаемые модули (Jinja2, SQLAlchemy):
Bootstrap - загогтовки для верстки фронтэнда и скрипты.
Awesome - набор векторных иконок для кнопок и т.д.
Blueprint - подмодули с подобной структурой (admin, blog, forum ...);
Flask-Script - обслуживающие скрипты (запуск сервера, загрузка БД данными ...);
Flask-Migrate - записи изменений БД (стукрута БД должна быть неизменной);
Flask-WTF - набор классов полей и валидаторов для форм;
Flask-Login - для авторизации и пользовательских сессий;
Flask-Bcrypt - для хеширования и проверки паролей; и т.д.